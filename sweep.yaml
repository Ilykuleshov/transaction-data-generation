program: main.py
method: grid
metric:
  name: val_loss
  goal: minimize
parameters:
  module.unfreeze_enc_after: 
    values: [5, 10, 15]
command:
  - ${env}
  - ${interpreter}
  - ${program}
  - ${args_no_hyphens}
