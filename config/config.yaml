defaults:
  - dataset: churn
  - encoder: coles_pretrained
  - decoder: lstm
  - module: vanilla

trainer_args:
  max_epochs: 50

datamodule_args:
  train_batch_size: 32
  val_batch_size: 8
  train_num_workers: 4
  val_num_workers: 4
