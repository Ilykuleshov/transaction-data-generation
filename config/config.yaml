defaults:
  - dataset: churn
  - encoder: coles_pretrained
  - decoder: lstm
  - module: vanilla

trainer_args:
  max_epochs: 50
  accumulate_grad_batches: 8
  gradient_clip_val: 1.

datamodule_args:
  train_batch_size: 16
  valid_batch_size: 8
  train_num_workers: 4
  valid_num_workers: 4
